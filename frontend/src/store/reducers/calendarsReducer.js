import personalCalendar from "../../mocks/calendar";

const initState = {
  allCalendars: [personalCalendar],
};

export const calendarsReducer = (state = initState, action) => {
  switch (action.type) {
  }
};
